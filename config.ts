// In a real-world application, these values would come from environment variables.
// For example, in a Create React App setup, you'd use process.env.REACT_APP_API_URL.
// This file centralizes configuration for easy management.

export const API_BASE_URL = '/api'; // Using a relative path is common for proxying in development.
